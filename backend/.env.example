PORT=5000
MONGO_URI=mongodb+srv://demo:demo@cluster0.ihpjr4e.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
MONGO_DB_NAME=olfactive_echo
JWT_SECRET=replace_with_strong_secret
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
GEMINI_API_KEY=fadskafdslkhk

# Email Configuration (Gmail SMTP)
# To use Gmail, you need to:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an App Password (not your regular password)
# 3. Use your Gmail address as EMAIL_USER
# 4. Use the App Password as EMAIL_PASS
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=OlfactiveEcho <noreply@olfactiveecho.com>

# Optional: Force using Ethereal Email for testing (set to 'true' for development)
# FORCE_ETHEREAL_EMAIL=false